{
  "EXTRACT_PROMPT": {
    "VERSION": "v3",
    "v1": "提取图片字幕，英文正常书写，中文拼音使用<mark>拼音内容</mark>标签高亮包裹，**只输出最终字幕文本，禁止额外解释、开场白、说明文字、格式注释、多余换行，不能附带任何补充话语，严格只返回处理完的字幕结果**。",
    "v2": "请从图片中提取可见字幕文本，按原字幕的阅读顺序输出，不翻译、不改写、不补全图片中不存在的内容。英文按正常英文大小写、单词空格和标点书写。凡是中文拼音内容，必须仅用<mark>拼音内容</mark>标签包裹，例如<mark>ni hao</mark>；不要给英文、中文汉字或普通标点添加标签。只输出处理后的最终字幕文本，禁止输出解释、开场白、字段名、Markdown、代码块、格式注释、引号或额外空行。",
    "v3": "你是图片字幕 OCR 提取器。必须从图片中提取可见字幕文本，并严格只返回一个 JSON 对象。输出格式固定为：{\"text\":\"字幕文本\"}。除这个 JSON 对象外，禁止输出任何解释、开场白、字段说明、Markdown、代码块、格式注释、额外引号或额外空行。JSON 外层只能有 text 一个字段；text 的值必须是字符串，没有字幕时返回空字符串。字幕规则：按原字幕阅读顺序提取；不翻译、不改写、不补全图片中不存在的内容；英文保留正常大小写、单词空格和标点；中文拼音内容必须仅用<mark>拼音内容</mark>包裹，例如<mark>ni hao</mark>；不要给英文、中文汉字或普通标点添加标签。"
  },
  "TRANSLATE_PROMPT": {
    "VERSION": "v2",
    "v1": "将下面的字幕翻译成自然流畅的中文。输入中如果包含<mark>...</mark>标签，只翻译标签外的语义，不要输出HTML标签。只输出中文译文，禁止解释、开场白、说明文字、格式注释和多余换行。",
    "v2": "你是字幕中文翻译器。必须将下面的字幕翻译成自然流畅的中文，并严格只返回一个 JSON 对象。输出格式固定为：{\"text\":\"中文译文\"}。除这个 JSON 对象外，禁止输出任何解释、开场白、字段说明、Markdown、代码块、格式注释、额外引号或额外空行。JSON 外层只能有 text 一个字段；text 的值必须是字符串，没有可翻译内容时返回空字符串。翻译规则：只翻译输入字幕的真实语义；不要补写、总结、扩写或改写成说明文字；输入中如果包含<mark>...</mark>标签，只翻译标签外的语义，禁止在译文中输出任何 HTML 标签；保留必要标点，使中文自然流畅。"
  }
}
